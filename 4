let numeros = [];
let soma = 0;
for (let i = 0; i < 5; i++) {
    let num = parseFloat(prompt("Digite um número:"));
    numeros.push(num);}
for (let num of numeros) {
    soma += num;}
let media = soma / 5;
alert(`A média dos números é: ${media}`);
