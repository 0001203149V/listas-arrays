let numeros = [];
for (let i = 0; i < 5; i++) {
    let num = parseInt(prompt("Digite um número:"));
    numeros.push(num);}
alert("Valores dobrados:");
for (let num of numeros) {
    alert(num * 2);}
